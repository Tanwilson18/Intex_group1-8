@page
@{
}

@model Burialmain

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @Model.Burialid
        </h4>
    </div>
    <div class="card-text p-1">@Model.Adultsubadult</div>
    <div class="card-text p-1">@Model.Squarenorthsouth</div>
    <div class="card-text p-1">@Model.Squareeastwest</div>
    <div class="card-text p-1">@Model.Area</div>
    <div class="card-text p-1">@Model.Burialnumber</div>
    <div class="card-text p-1">@Model.Area</div>

</div>